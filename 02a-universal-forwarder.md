# Module 2a — Universal Forwarder (UF) Deep Dive

> **Status: 🔜 Coming next**

## Topics Covered Here
- What the UF binary actually is (stripped Splunk — no indexer, no search)
- `inputs.conf` — file monitoring, Windows Event Log, scripted inputs, network (UDP/TCP)
- `outputs.conf` — indexer targets, load balancing, SSL/TLS
- Deployment at scale via Deployment Server
- Resource profile: RAM, CPU, disk
- Common failure modes: queue full, TLS mismatch, Deployment Server unreachable
- CLI commands for UF management
- Cert exam touchpoints

---

*Content added when we reach this module.*
